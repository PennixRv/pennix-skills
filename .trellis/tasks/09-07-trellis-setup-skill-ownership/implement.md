# Implementation

1. Replace user-level references to `trellis-research-record` with the
   project-bundled ownership statement.
2. Remove the retired Skill and add the minimal native setup entry point.
3. Update installer expectations, validate source Skills, and run installer
   tests.
4. After the paired Trellis release, push this repository and deploy using the
   existing collection installer.
